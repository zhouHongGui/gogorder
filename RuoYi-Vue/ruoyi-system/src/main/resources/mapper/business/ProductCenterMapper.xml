<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.ProductCenterMapper">

    <resultMap id="CategoryResult" type="Category">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <select id="selectCategoryList" parameterType="Category" resultMap="CategoryResult">
        select id, name, sort_order, status, create_time from category
        <where>
            <if test="name != null and name != ''">name like concat('%', #{name}, '%')</if>
            <if test="status != null">and status = #{status}</if>
        </where>
        order by sort_order asc, id asc
    </select>

    <select id="selectCategoryById" parameterType="Long" resultMap="CategoryResult">
        select id, name, sort_order, status, create_time from category where id = #{id}
    </select>

    <insert id="insertCategory" parameterType="Category" useGeneratedKeys="true" keyProperty="id">
        insert into category(name, sort_order, status, create_time)
        values(#{name}, #{sortOrder}, #{status}, sysdate())
    </insert>

    <update id="updateCategory" parameterType="Category">
        update category set name = #{name}, sort_order = #{sortOrder}, status = #{status} where id = #{id}
    </update>

    <delete id="deleteProductCategoriesByCategoryId" parameterType="Long">
        delete from product_category where category_id = #{categoryId}
    </delete>

    <delete id="deleteCategoryById" parameterType="Long">
        delete from category where id = #{id}
    </delete>

    <resultMap id="SpecTemplateResult" type="SpecTemplate">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="type" column="type"/>
        <result property="isRequired" column="is_required"/>
        <result property="minSelect" column="min_select"/>
        <result property="maxSelect" column="max_select"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="optionCount" column="option_count"/>
        <result property="defaultOptionCount" column="default_option_count"/>
    </resultMap>

    <sql id="selectSpecTemplateVo">
        select st.id, st.name, st.type, st.is_required, st.min_select, st.max_select,
               st.sort_order, st.status, st.create_time,
               (select count(1) from spec_option so where so.template_id = st.id) option_count,
               (select count(1) from spec_option so where so.template_id = st.id and so.is_default = 1 and so.status = 1) default_option_count
        from spec_template st
    </sql>

    <select id="selectSpecTemplateList" parameterType="SpecTemplate" resultMap="SpecTemplateResult">
        <include refid="selectSpecTemplateVo"/>
        <where>
            <if test="name != null and name != ''">st.name like concat('%', #{name}, '%')</if>
            <if test="status != null">and st.status = #{status}</if>
        </where>
        order by st.sort_order asc, st.id asc
    </select>

    <select id="selectSpecTemplateById" parameterType="Long" resultMap="SpecTemplateResult">
        <include refid="selectSpecTemplateVo"/> where st.id = #{id}
    </select>

    <insert id="insertSpecTemplate" parameterType="SpecTemplate" useGeneratedKeys="true" keyProperty="id">
        insert into spec_template(name, type, is_required, min_select, max_select, sort_order, status, create_time)
        values(#{name}, #{type}, #{isRequired}, #{minSelect}, #{maxSelect}, #{sortOrder}, #{status}, sysdate())
    </insert>

    <update id="updateSpecTemplate" parameterType="SpecTemplate">
        update spec_template
        set name = #{name}, type = #{type}, is_required = #{isRequired}, min_select = #{minSelect},
            max_select = #{maxSelect}, sort_order = #{sortOrder}, status = #{status}
        where id = #{id}
    </update>

    <select id="countProductsByTemplateId" parameterType="Long" resultType="int">
        select count(1) from product
        where JSON_CONTAINS(spec_template_ids, JSON_EXTRACT(concat('[', #{templateId}, ']'), '$[0]'))
    </select>

    <select id="countHistoricalOptionsByTemplateId" parameterType="Long" resultType="int">
        select count(1)
        from biz_order_item oi
        where exists (
            select 1 from spec_option so
            where so.template_id = #{templateId}
              and JSON_SEARCH(oi.specs, 'one', so.option_id) is not null
        )
    </select>

    <delete id="deleteSpecOptionsByTemplateId" parameterType="Long">
        delete from spec_option where template_id = #{templateId}
    </delete>

    <delete id="deleteSpecTemplateById" parameterType="Long">
        delete from spec_template where id = #{id}
    </delete>

    <resultMap id="SpecOptionResult" type="SpecOption">
        <id property="id" column="id"/>
        <result property="optionId" column="option_id"/>
        <result property="templateId" column="template_id"/>
        <result property="label" column="label"/>
        <result property="priceAdd" column="price_add"/>
        <result property="isDefault" column="is_default"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <select id="selectSpecOptionList" parameterType="Long" resultMap="SpecOptionResult">
        select id, option_id, template_id, label, price_add, is_default, sort_order, status, create_time
        from spec_option where template_id = #{templateId}
        order by sort_order asc, id asc
    </select>

    <select id="selectSpecOptionById" parameterType="Long" resultMap="SpecOptionResult">
        select id, option_id, template_id, label, price_add, is_default, sort_order, status, create_time
        from spec_option where id = #{id}
    </select>

    <select id="countDefaultOptions" resultType="int">
        select count(1) from spec_option
        where template_id = #{templateId} and is_default = 1 and status = 1
        <if test="excludeId != null">and id != #{excludeId}</if>
    </select>

    <insert id="insertSpecOption" parameterType="SpecOption" useGeneratedKeys="true" keyProperty="id">
        insert into spec_option(option_id, template_id, label, price_add, is_default, sort_order, status, create_time)
        values(#{optionId}, #{templateId}, #{label}, #{priceAdd}, #{isDefault}, #{sortOrder}, #{status}, sysdate())
    </insert>

    <update id="updateSpecOption" parameterType="SpecOption">
        update spec_option
        set label = #{label}, price_add = #{priceAdd}, is_default = #{isDefault},
            sort_order = #{sortOrder}, status = #{status}
        where id = #{id}
    </update>

    <update id="disableSpecOption" parameterType="Long">
        update spec_option set status = 0, is_default = 0 where id = #{id}
    </update>

    <select id="countHistoricalOptionReferences" parameterType="String" resultType="int">
        select count(1) from biz_order_item where JSON_SEARCH(specs, 'one', #{optionId}) is not null
    </select>

    <resultMap id="ProductResult" type="Product">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="image" column="image"/>
        <result property="imagesJson" column="images"/>
        <result property="description" column="description"/>
        <result property="basePrice" column="base_price"/>
        <result property="specTemplateIdsJson" column="spec_template_ids"/>
        <result property="tagsJson" column="tags"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="selectProductVo">
        select p.id, p.name, p.image, p.images, p.description, p.base_price, p.spec_template_ids,
               p.tags, p.status, p.create_time, p.update_time
        from product p
    </sql>

    <select id="selectProductList" parameterType="Product" resultMap="ProductResult">
        <include refid="selectProductVo"/>
        <where>
            <if test="name != null and name != ''">p.name like concat('%', #{name}, '%')</if>
            <if test="status != null">and p.status = #{status}</if>
            <if test="categoryId != null">
                and exists (select 1 from product_category pc where pc.product_id = p.id and pc.category_id = #{categoryId})
            </if>
        </where>
        order by p.id desc
    </select>

    <select id="selectProductById" parameterType="Long" resultMap="ProductResult">
        <include refid="selectProductVo"/> where p.id = #{id}
    </select>

    <select id="countProductsByIds" resultType="int">
        select count(1) from product where id in
        <foreach collection="list" item="id" open="(" separator="," close=")">#{id}</foreach>
    </select>

    <select id="countCategoriesByIds" resultType="int">
        select count(1) from category where id in
        <foreach collection="list" item="id" open="(" separator="," close=")">#{id}</foreach>
    </select>

    <select id="countSpecTemplatesByIds" resultType="int">
        select count(1) from spec_template where id in
        <foreach collection="list" item="id" open="(" separator="," close=")">#{id}</foreach>
    </select>

    <insert id="insertProduct" parameterType="Product" useGeneratedKeys="true" keyProperty="id">
        insert into product(name, image, images, description, base_price, spec_template_ids, tags, status, create_time)
        values(#{name}, #{image}, #{imagesJson}, #{description}, #{basePrice}, #{specTemplateIdsJson}, #{tagsJson}, #{status}, sysdate())
    </insert>

    <update id="updateProduct" parameterType="Product">
        update product
        set name = #{name}, image = #{image}, images = #{imagesJson}, description = #{description},
            base_price = #{basePrice}, spec_template_ids = #{specTemplateIdsJson}, tags = #{tagsJson},
            status = #{status}, update_time = sysdate()
        where id = #{id}
    </update>

    <delete id="deleteProductCategoriesByProductId" parameterType="Long">
        delete from product_category where product_id = #{productId}
    </delete>

    <insert id="insertProductCategories">
        insert into product_category(product_id, category_id, sort_order) values
        <foreach collection="categoryIds" item="categoryId" separator=",">
            (#{productId}, #{categoryId}, 0)
        </foreach>
    </insert>

    <select id="selectCategoryIdsByProductId" parameterType="Long" resultType="long">
        select category_id from product_category where product_id = #{productId} order by id asc
    </select>

    <select id="selectCategoriesByProductId" parameterType="Long" resultMap="CategoryResult">
        select c.id, c.name, c.sort_order, c.status, c.create_time
        from category c inner join product_category pc on pc.category_id = c.id
        where pc.product_id = #{productId}
        order by pc.sort_order asc, c.sort_order asc
    </select>

    <select id="countShopProductsByProductId" parameterType="Long" resultType="int">
        select count(1) from shop_product where product_id = #{productId}
    </select>

    <delete id="deleteProductById" parameterType="Long">
        delete from product where id = #{id}
    </delete>

    <resultMap id="ShopProductResult" type="ShopProduct">
        <id property="id" column="id"/>
        <result property="shopId" column="shop_id"/>
        <result property="productId" column="product_id"/>
        <result property="price" column="price"/>
        <result property="status" column="status"/>
        <result property="stock" column="stock"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="createTime" column="create_time"/>
        <result property="productName" column="product_name"/>
        <result property="productImage" column="product_image"/>
        <result property="basePrice" column="base_price"/>
        <result property="effectivePrice" column="effective_price"/>
        <result property="shopName" column="shop_name"/>
    </resultMap>

    <select id="selectShopProductList" parameterType="Long" resultMap="ShopProductResult">
        select sp.id, sp.shop_id, sp.product_id, sp.price, sp.status, sp.stock, sp.sort_order, sp.create_time,
               p.name product_name, p.image product_image, p.base_price,
               coalesce(sp.price, p.base_price) effective_price, s.name shop_name
        from shop_product sp
        inner join product p on p.id = sp.product_id
        inner join shop s on s.id = sp.shop_id and s.del_flag = 0
        where sp.shop_id = #{shopId}
        order by sp.sort_order asc, sp.id desc
    </select>

    <select id="selectShopProductById" parameterType="Long" resultMap="ShopProductResult">
        select sp.id, sp.shop_id, sp.product_id, sp.price, sp.status, sp.stock, sp.sort_order, sp.create_time,
               p.name product_name, p.image product_image, p.base_price,
               coalesce(sp.price, p.base_price) effective_price, s.name shop_name
        from shop_product sp
        inner join product p on p.id = sp.product_id
        inner join shop s on s.id = sp.shop_id
        where sp.id = #{id}
    </select>

    <select id="selectShopProductByIdForUpdate" parameterType="Long" resultMap="ShopProductResult">
        select sp.id, sp.shop_id, sp.product_id, sp.price, sp.status, sp.stock, sp.sort_order, sp.create_time
        from shop_product sp where sp.id = #{id} for update
    </select>

    <select id="countShopById" parameterType="Long" resultType="int">
        select count(1) from shop where id = #{shopId} and del_flag = 0
    </select>

    <insert id="assignShopProducts">
        insert ignore into shop_product(shop_id, product_id, price, status, stock, sort_order, create_time) values
        <foreach collection="productIds" item="productId" separator=",">
            (#{shopId}, #{productId}, null, 1, -1, 0, sysdate())
        </foreach>
    </insert>

    <update id="updateShopProduct" parameterType="ShopProduct">
        update shop_product set price = #{price}, status = #{status}, sort_order = #{sortOrder} where id = #{id}
    </update>

    <update id="updateShopProductStock">
        update shop_product set stock = #{stock} where id = #{id}
    </update>

    <update id="deductShopProductStock">
        update shop_product
        set stock = case when stock = -1 then -1 else stock - #{quantity} end
        where id = #{id} and (stock = -1 or stock &gt;= #{quantity})
    </update>

    <delete id="deleteShopProducts">
        delete from shop_product where id in
        <foreach collection="list" item="id" open="(" separator="," close=")">#{id}</foreach>
    </delete>

    <resultMap id="StockLedgerResult" type="StockLedger">
        <id property="id" column="id"/>
        <result property="shopProductId" column="shop_product_id"/>
        <result property="changeType" column="change_type"/>
        <result property="changeAmount" column="change_amount"/>
        <result property="beforeStock" column="before_stock"/>
        <result property="afterStock" column="after_stock"/>
        <result property="orderId" column="order_id"/>
        <result property="idempotentKey" column="idempotent_key"/>
        <result property="reason" column="reason"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <select id="selectStockLedgerByKey" parameterType="String" resultMap="StockLedgerResult">
        select id, shop_product_id, change_type, change_amount, before_stock, after_stock,
               order_id, idempotent_key, reason, create_time
        from stock_ledger where idempotent_key = #{idempotentKey}
    </select>

    <insert id="insertStockLedger" parameterType="StockLedger" useGeneratedKeys="true" keyProperty="id">
        insert into stock_ledger(shop_product_id, change_type, change_amount, before_stock, after_stock,
                                 order_id, idempotent_key, reason, create_time)
        values(#{shopProductId}, #{changeType}, #{changeAmount}, #{beforeStock}, #{afterStock},
               #{orderId}, #{idempotentKey}, #{reason}, sysdate())
    </insert>
</mapper>
