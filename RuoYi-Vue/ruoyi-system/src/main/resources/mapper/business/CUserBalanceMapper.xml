<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.CUserBalanceMapper">
    <resultMap id="CUserBalanceResult" type="CUserBalance">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="balance" column="balance"/>
        <result property="version" column="version"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>
    <select id="selectByUserId" parameterType="Long" resultMap="CUserBalanceResult">
        select id, user_id, balance, version, create_time, update_time from c_user_balance where user_id = #{userId}
    </select>
    <select id="selectByUserIdForUpdate" parameterType="Long" resultMap="CUserBalanceResult">
        select id, user_id, balance, version, create_time, update_time
        from c_user_balance where user_id = #{userId} for update
    </select>
    <update id="updateBalance">
        update c_user_balance set balance = balance + #{delta}, version = version + 1, update_time = sysdate()
        where user_id = #{userId} and version = #{version}
    </update>
</mapper>
