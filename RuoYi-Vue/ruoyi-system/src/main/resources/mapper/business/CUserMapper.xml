<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.CUserMapper">

    <resultMap id="CUserResult" type="CUser">
        <id property="id" column="id"/>
        <result property="phone" column="phone"/>
        <result property="nickname" column="nickname"/>
        <result property="avatar" column="avatar"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectById" parameterType="Long" resultMap="CUserResult">
        select id, phone, nickname, avatar, status, create_time, update_time
        from c_user where id = #{id}
    </select>

    <select id="selectByPhone" parameterType="String" resultMap="CUserResult">
        select id, phone, nickname, avatar, status, create_time, update_time
        from c_user where phone = #{phone}
    </select>

    <insert id="insertUserIfAbsent" parameterType="String">
        insert ignore into c_user(phone, nickname, avatar, status, create_time)
        values(#{phone}, '', '', 1, sysdate())
    </insert>

    <insert id="insertBalanceIfAbsent" parameterType="Long">
        insert ignore into c_user_balance(user_id, balance, version, create_time, update_time)
        values(#{userId}, 0, 0, sysdate(), sysdate())
    </insert>

    <update id="updateUser" parameterType="CUser">
        update c_user
        <set>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            update_time = sysdate()
        </set>
        where id = #{id} and status = 1
    </update>

    <resultMap id="CUserWechatResult" type="CUserWechat">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="platform" column="platform"/>
        <result property="openid" column="openid"/>
        <result property="unionid" column="unionid"/>
    </resultMap>

    <select id="selectWechatByOpenid" resultMap="CUserWechatResult">
        select id, user_id, platform, openid, unionid
        from c_user_wechat
        where platform = #{platform} and openid = #{openid}
    </select>

    <insert id="insertWechatIfAbsent" parameterType="CUserWechat">
        insert ignore into c_user_wechat(user_id, platform, openid, unionid, create_time)
        values(#{userId}, #{platform}, #{openid}, #{unionid}, sysdate())
    </insert>
</mapper>
