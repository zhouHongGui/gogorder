# gogorder 多门店点单系统 — V1.0 PRD

---

## 一、项目概述

### 1.1 产品名称
**gogorder 多门店点单系统**

### 1.2 产品定位
面向奶茶/饮品连锁品牌的多门店在线点单系统。C端采用 **uni-app** 开发，同时发布**微信小程序**和 **H5网页版**。B端门店操作台和平台管理后台以**若依管理系统**作为底层框架进行二次开发，使用 **Vue 3 + Vite 6**。

### 1.3 目标用户

| 角色 | 使用终端 |
|------|---------|
| C端顾客 | uni-app（微信小程序 + H5手机浏览器） |
| 门店店员 | Vue 3 前端（Pad/PC），默认绑定一家门店 |
| 店长/多店授权岗 | Vue 3 前端（Pad/PC），可关联多家门店并切换 |
| 平台管理员 | Vue 3 前端（PC） |

### 1.4 技术栈

| 层级 | 技术 |
|------|------|
| C端 | uni-app（Vue 3） |
| B端/管理后台 | Vue 3 + Vite 6 + Element Plus |
| 后端 | Java 21 + Spring Boot 4.0.3（若依 RuoYi-Vue 作为底层框架） |
| 数据库 | MySQL 8.0 |
| 缓存 | Redis 5.0.14.1 |

### 1.5 V1.0 核心交付
顾客选择门店 → 浏览菜单 → 加购 → 下单 → **余额支付** → 门店接单 → 制作 → 叫号 → 核销取餐。

---

## 二、V1.0 版本范围

**包含**：C端下单全流程 + 余额支付 + 门店接单核销 + 门店/商品/订单管理 + 基础统计

**不包含**：外卖配送、微信支付、优惠券/满减/积分、小票打印、会员体系、SKU规格库存、原料库存

---

## 三、C端功能（uni-app）

### 3.1 登录
- **小程序**：`uni.login` 获取 code → 后端换 openid；手机号通过新版 `getPhoneNumber` 获取 phone code，后端调用微信接口解密。登录 code 与手机号 code 分开传递。
- **H5**：手机号 + 短信验证码登录，不依赖公众号 openid/JSAPI。
- **账号统一**：手机号为核心标识。微信身份存入独立表 `c_user_wechat`。

### 3.2 门店选择
- LBS 定位推荐附近门店，按距离排序。
- 营业时间支持跨午夜（如 18:00–02:00）。
- 休息中/暂停接单的门店可见但标注状态且不可下单。
- 门店使用逻辑删除。

### 3.3 菜单
- 商品分类左侧导航 + 商品列表右侧展示。
- 商品支持**多分类**（关联表 `product_category`）。
- 规格选择传递**稳定的 optionId**，价格由后端重算。
- 展示价 = 该门店最低可能价格。

### 3.4 购物车
- Redis 存储。添加接口**必须携带 shopId**，门店不一致返回错误（不清空）。
- 每个条目用独立 Hash Field 或 Lua 脚本解决并发问题。
- 切换门店时弹窗确认，用户确认后前端调清空接口。

### 3.5 下单与支付（余额支付）
- 下单校验：门店营业状态、营业时间（含跨午夜）、商品上架、库存充足。
- **余额支付流程**：
  1. 提交订单 → 创建订单 + 原子扣减库存
  2. 调余额支付接口 → 行级锁扣款 → 写入支付流水和余额流水（直接扣款，无需支付密码）
  3. 支付幂等键防重
  4. 余额不足返回错误（提示联系店员充值）
  5. V1.0 仅后台手动充值，C端无充值入口。新用户初始余额为 0。
- 15分钟未支付自动取消，恢复库存。
- 取消/退款：恢复库存 + 恢复余额，幂等键防重复。

### 3.6 订单中心
- Tab：全部 / 待支付 / 进行中 / 已完成 / 已取消。
- 订单状态拆分展示（order_status + pay_status）。
- 取餐码：`pickup_token`（核销用二维码）+ `pickup_display`（展示叫号用）。
- 操作：去支付、取消、查看取餐码、再来一单。

### 3.7 个人中心
- 余额展示、订单入口、地址（预留）、设置。

---

## 四、门店端（Vue 3 · Pad/PC）

- **三列看板**：待接单 / 制作中 / 待取餐。
- **新订单提醒**：5秒轮询 + 声音。
- **接单** → **制作完成（叫号）** → **扫码/手输核销**。
- **多门店支持**：店长和授权岗可切换当前操作门店。
- **越权防护**：所有接口校验 `staff_shop` 关联。

---

## 五、管理后台（Vue 3 · PC）

- **门店管理**：CRUD + 状态 + 逻辑删除 + 营业时间跨午夜 + 排序。
- **商品中心**：分类 / 规格模板（optionId 不可变） / 商品库 / 门店分配 + 差异化定价 / 商品级库存。
- **订单管理**：全平台查询 + 取消退款。
- **数据统计**：今日概览 + 近7日趋势 + 门店排行。

---

## 六、核心数据表

| 表 | 用途 |
|----|------|
| `c_user` | C端用户（手机号唯一） |
| `c_user_wechat` | 微信身份绑定 |
| `c_user_balance` | 余额账户 |
| `balance_ledger` | 余额流水 |
| `payment_ledger` | 支付流水（含幂等键） |
| `refund_ledger` | 退款流水（含幂等键） |
| `shop` | 门店（逻辑删除） |
| `staff_shop` | 员工-门店多对多 |
| `category` | 商品分类 |
| `product_category` | 商品-分类多对多 |
| `spec_template` | 规格模板 |
| `spec_option` | 规格选项（全局唯一 optionId） |
| `product` | 商品库 |
| `shop_product` | 门店商品（库存+差异化价格） |
| `stock_ledger` | 库存流水（含幂等键） |
| `biz_order` | 订单（三状态拆分） |
| `biz_order_item` | 订单明细（规格快照） |

---

## 七、关键设计决策

- **余额支付**：行级锁 + 乐观锁 + 幂等键 + 流水双写
- **库存**：条件 UPDATE 原子扣减 + 库存流水 + 幂等恢复
- **取餐码**：内部令牌（pickup_token）与展示号（pickup_display）分离
- **规格**：optionId 全局唯一不可变，订单快照存储完整信息
- **多分类**：商品-分类关联表
- **多门店**：员工-门店关联表 + 当前门店上下文 + 越权校验
- **购物车**：shopId 必传，并发用 Lua/分 Field
- **monthly_sales**：从订单表实时查询近30天

> 详细设计见 `docs/` 目录下 M00–M13 模块文档，全局枚举与契约以 M13 为准。
