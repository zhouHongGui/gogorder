# M09 - C端订单中心与个人中心

---

## 1. 概述

C端用户查看订单列表、订单详情（含三状态 + 取餐码）、执行订单操作。个人中心展示余额和基础功能入口。

**所属端**：C端（uni-app）+ 后端

**优先级**：🟡 中 — 依赖 M08 先完成

---

## 2. 功能列表

| 编号 | 功能 |
|------|------|
| F01 | 订单列表（Tab：全部/待支付/进行中/已完成/已取消） |
| F02 | 订单详情（三状态时间线 + 取餐码） |
| F03 | 去支付 / 取消订单 / 查看取餐码 |
| F04 | 再来一单 |
| F05 | 个人中心（余额展示 + 订单入口） |

---

## 3. 设计逻辑

### 3.1 订单状态与操作（对齐 M13）

| order_status | C端标签 | 可操作 |
|-------------|---------|--------|
| 0 | 待支付 | 去支付、取消 |
| 1 | 等待接单 | — |
| 2 | 制作中 | — |
| 3 | 待取餐 | 查看取餐码 |
| 4 | 已完成 | 再来一单 |
| 5 | 已取消 | 再来一单 |

### 3.2 取餐码展示（对齐 M13）

- **pickup_token**：转二维码，用于门店扫码核销。
- **pickup_display**：大字展示，用于用户看叫号屏/听叫号。
- 仅在 `order_status=3` 时展示。

### 3.3 再来一单

- 将 `biz_order_item` 中的商品+规格（含 optionId）重新加入购物车。
- 使用当前最新价格。
- 已下架商品跳过并提示。
- 购物车 shopId 不一致时弹窗询问用户。

### 3.4 个人中心

- 顶部展示头像 + 昵称 + 手机号。
- **余额展示**：调用 `/api/c/user/balance` 获取当前余额。
- 入口：我的订单、余额明细（流水列表）、设置。

---

## 4. 接口定义

### 4.1 订单列表

```
GET /api/c/order/list?status=-1&pageNum=1&pageSize=10
```

status：不传=全部，0=待支付，-1=进行中(1,2,3)，4=已完成，5=已取消

### 4.2 订单详情

```
GET /api/c/order/{id}
```

响应包含：三状态字段、时间线、明细、取餐码（token+display）。

### 4.3 再来一单

```
POST /api/c/order/repurchase/{id}
```

### 4.4 余额查询

```
GET /api/c/user/balance
Response: { "balance": 50000 }
```

---

## 5. 前置依赖
- M05 - 登录
- M08 - 下单支付

## 6. 被依赖模块
- M10 - 门店端（同一份订单）

## 7. 验收标准
- [ ] 订单列表 Tab 切换正常，分页加载正常
- [ ] 订单详情三状态 + 时间线完整展示
- [ ] 取餐码仅在待取餐时展示（token 二维码 + display 数字）
- [ ] 再来一单使用最新价格 + 跳过已下架商品
- [ ] 余额展示正确
- [ ] 取消订单后状态正确更新
