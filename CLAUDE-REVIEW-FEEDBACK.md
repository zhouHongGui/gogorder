# Claude Code 需求文档审查反馈

请先修订 `PRD-V1.0.md` 与 `docs/M00-M12`，暂时不要编写代码。

## 已确认产品决策

1. V1.0 使用用户余额支付，不使用微信支付。
2. V1.0 使用商品级库存：
   - 库存在 `shop_product` 维度管理。
   - `-1` 表示无限库存。
   - 有限库存下单时原子扣减，取消订单时恢复。
   - 需要库存流水、并发控制和重复释放防护。
   - 暂不实现 SKU 规格库存和原料库存。
3. H5 支持普通手机浏览器，以手机号短信登录为主。
4. 员工默认属于一家门店；店长及被授权岗位允许关联多家门店并切换当前门店。
5. 商品允许同时属于多个分类。

## 必须修正

1. 实际技术栈为 Java 21、Spring Boot 4.0.3、Vue 3、Vite 6、Redis 5.0.14.1。产品名为 `gogorder`，若依仅作为底层框架来源。
2. 补充员工与门店多对多关联、默认门店、多店岗位授权、当前门店上下文和门店数据越权测试。
3. 将 SQL 关键字表名 `order`、`order_item` 改为 `biz_order`、`biz_order_item`。
4. 拆分订单状态、余额支付状态、退款状态；增加余额账户、余额流水、支付流水与退款流水。
5. 余额支付必须覆盖扣款原子性、幂等、并发支付、余额不足、取消退款、重复退款防护、账实一致性和对账。
6. 商品级库存必须覆盖原子条件扣减、库存流水、取消恢复、重复释放防护和并发验收。删除规格选项库存描述。
7. 修复取餐码跨年和跨门店碰撞问题，将内部唯一核销令牌与门店日内展示号分开。
8. 微信小程序手机号授权使用新版动态 phone code；登录 code 与手机号 code 分开。
9. 普通 H5 不依赖公众号 openid 或微信 JSAPI。若保留微信身份，使用独立身份绑定表。
10. 购物车添加接口必须携带 `shopId`；门店不一致返回错误，不能静默清空。
11. 规格选择传稳定 `optionId`，价格必须由后端重新计算。
12. Redis 购物车整段 JSON 读改写需要解决并发丢更新问题。
13. 商品改为多分类关联表，包含分类内排序、唯一约束、删除规则与查询接口。
14. 统一休息中门店是否可见；营业时间支持跨午夜。
15. 门店使用逻辑删除并保留历史订单引用，禁止级联删除历史数据。
16. 统一若依分页响应、Token 认证和三端接口契约。
17. 明确规格模板修改策略、商品可用选项、不可变 `optionId` 和订单规格价格快照。
18. `monthly_sales` 改为具有明确统计窗口的查询或日汇总方案。
19. 新增“全局枚举与接口契约”作为单一事实来源，并逐篇对齐状态、字段、接口与范围。

## 修订完成后的输出

- 列出修改过的文件。
- 总结关键修正。
- 仅列出仍然真实存在的待确认事项。
- 未完成文档修订前不要开始代码实现。
